---
description: JSX prose: explicit {" "} after inline tags so words do not glue
globs: "**/*.{tsx,jsx}"
alwaysApply: false
---

# JSX inline-tag spacing

JSX line breaks around inline tags are not reliable spaces (`greenside`, `them).The`).

Between preceding text and a following inline tag (`<strong>`, `<dfn>`, `<em>`, `<b>`, `<span>`, `<code>`, etc.), use `{" "}` so the space is not lost across a line break (e.g. `The{" "}<strong>`). Same after a period/paren before a following tag. After a closing inline tag, if more text follows, use `{" "}` as well.

```tsx
The{" "}
<strong className="font-semibold text-slate-900">green</strong>
{" "}
side is the state&apos;s record.
```

Prefer `</strong>{" "}else` so formatters cannot drop the space. Re-read edited prose as plain text.
