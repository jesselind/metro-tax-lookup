# Locked decisions (persistent)

Tracked maintainer memory (`docs/locked-decisions.md`). **Not** under gitignored `docs/_working/` — that folder is urgency backlog only (delete topic files when done).

**Purpose:** Durable record of author product / UX / copy calls across chats. Helps agents avoid re-litigating settled choices.

**Not a whitelist:** Final checks and reviews follow `.cursor/rules/base-rule.mdc`: **ask first** for UI/UX that changes layout, visual design, disclosure, column order, labels/copy/voice, or how residents move; **fix without asking** for same-product engineering and in-place accessibility that does not change the resident experience. Missing a row here does **not** mean the agent may redesign the UI. When in doubt, ask.

**How to use:** Short rows only. No secrets. No private personal data. Prefer code paths over essays. Urgency backlog index: `docs/_working/00-backlog.md`.

| Date | Decision | Notes |
|------|----------|-------|
| 2026-09-17 | Face Est. property tax $ === mill-levy stack $ under `levyStackDollars` (dual-base when school ≠ local). Never a separate local × total mills face. Douglas keeps `realwareTaxDollars`; stack reconciles to Realware face. | `.cursor/rules/property-tax-estimate-honesty.mdc` |
| 2026-09-16 | Semver: at most one `package.json` bump per branch; fold close-out into the same changelog entry. | `.cursor/rules/base-rule.mdc` |
| 2026-09-15 | Douglas face $: Realware `taxDollars` + `alternateTaxDollars` only; never silent `assessed × total mills`. Resident label **Est. property tax** (not Est. on every `$`). Dual-rate school/local bases county-agnostic. (Arapahoe face mode superseded 2026-09-17 → `levyStackDollars`.) | `.cursor/rules/property-tax-estimate-honesty.mdc` |
| 2026-09-08 | Account kind classifier is county-agnostic vocabulary union: Arapahoe Real/Improvement **and** Douglas Residential/Commercial/Industrial/… → `real_property`; Personal/PERSPROP → BPP; Possessory* → other. **Switch account type** still requires real_property + BPP (not other+BPP). Do not use `features.bpp` to gate the switch. | `classifySitusPinAccountKind`, `douglas-vs-arapahoe-resident-features.md`. |
| 2026-09-08 | Renter **N**: Land Line `UB` sum → building `Units` attr sum (Douglas `No_Of_Unit`) → duplex/triplex/fourplex Improvement Type → N=1 single-dwelling → else unknown. Never invent N from "9+" apartment text. | `resolveDwellingCount.ts`; supersedes 2026-08-12 N order row for building-units insert. |
| 2026-08-16 | Valuation / sales-histories: no scrape; no AVM. NBHD from official Open GIS Parcels GDB (`supporting-data/county-gis/Assessor_Parcels_SP.gdb`). **MVP-1:** join PIN→neighborhoodCode/name into parcel-record shards (fill existing Property details slots) + `/sources` cite. **Not MVP:** county CurrentSales deep-link as primary UX (awful county page; we control presentation). **Next after join:** in-app neighborhood sales from official exports. Comps PDF / NOV grid parked until ~2027 Assessor return. Subject sale history keep-as-is (not initiative center). Prior-year assessed ("value vs mills") is a goal; not in mart or GIS snapshot—needs archive/request. First GDB ship: PIN + NBHD code/name only. | (sales-histories working note deleted or superseded; comps → `docs/_working/04-douglas-comps-pdf-url-and-parse.md`) |
| 2026-08-16 | Authority-chain PDF cites: prefer verified `#page=N` (viewer index) when the claim is on a known page. No fragment → viewer opens at page 1; that is an honest fallback, not a broken link. Never invent a page number. Levy % rate-table cites are property-specific at render: tax year + AUTH + zero-padded parcel `tagShortDescr` → `#page=N` from `arapahoe-authority-rate-table-pages.json` (`0501`/`0601`/`2998`/`4571`/`4100`). Missing historical TAG+AUTH → year PDF without fragment (same page-1 fallback). Re-run extractor when adding AUTH. | `authorityMillsHistory.ts`, `LevyAuthorityChainSection`, extractor, authoring doc |
| 2026-08-16 | SMFR closed summary: `summaryClosingNote` → `NOTE:` on next line; Ballot Issue links first occurrence only; Dougco Notice is 7A wording cite while Arapahoe Notice PDF is not currently available (multi-county). No 404 language; no source-fallback popover. | `south-metro-fire-authority-chain`, `summaryClosingNote` |
| 2026-08-16 | Fire authority-chain family: Voters + Ballot Issue + Arapahoe certified votes (school-like trail); AUTH-derived What changed? (metro-like mills via `usesAuthDerivedMills`). Kinds: `operations_mill` / `bond` / `tabor_revenue_retention`. openGap `multi-county-arapahoe-votes-only` when tallies are Arapahoe-only. First entry: SMFR `4100` Ballot Issue 7A (Nov 2025). Do not conflate AUTH `4100` with Cherry Hills Fire bond `4161`. | `FIRE_PACK`, `south-metro-fire-authority-chain` |
| 2026-08-15 | Resident copy: keep the **real term** and gloss in place; do not paraphrase away a term the app defines elsewhere (popover / glossary / documented actor). Readability-tool suggestions follow intent, not literal term swaps. Common sense over rigid rule reading. | `.cursor/rules/base-rule.mdc` Resident-facing copy |
| 2026-08-15 | Metro What changed? trail body = short bill takeaway only; do **not** narrate Change from last year / Most notable selection rules in `mills.stepBody` (methodology on `/sources`). Pack default: "Your bill uses one total mill rate for this district each year." | `METRO_MILLS_STEP_BODY`, Sky Ranch `mills: {}`, authoring doc |
| 2026-08-15 | Authority-chain facts may carry optional `valueTermId` / `valueTermMatch` (fact-value glosses). Sky Ranch Dec 2020 service-plan fact uses `term-aggregate-debt`. Metro: `ballotIssue` forbidden on `metro_commitment`; approval vs votes+resultsSource mutually exclusive; Ballot Issue vote-label prefix only when `ballotIssue` set. | `LevyAuthorityChainFact`, validate/build, `GlossaryTermPopover` |
| 2026-08-15 | Sky Ranch whoGetsBody: bill-first CAB transfer for ops / regional improvements / CAB bond payments; no uncited "shared board" / duplicate transfer narration. Eligible-electors brief: statutory paths (property owners + registered residents), not board affiliation. `/sources` keeps "eligible electors" + in-place gloss. | `sky-ranch-3-metro-authority-chain`, `TermEligibleElectorsBriefBody`, `/sources` |
| 2026-08-15 | Metro: when ballot wording is unavailable but cited `approval` exists, omit duplicate Election-record fact (body + `metro-election-records-unlocated` already cover it). `metro_commitment` has no ballot/election-record fact. Approval facts before `supportingFacts`. Levy % cites from AUTH JSON `residentUrl` only (no second URL map / no `rateSourcesByTaxYear`). | `levyAuthorityChainBuild.ts`, authoring doc |
| 2026-08-15 | Metro mills DRY: trail Change from last year / Most notable change derived from AUTH mills series (same source as chart); no hand-authored metro mill years/values; most notable = max \|\Delta\| adjacent YoY, omit if same as last year, ties → more recent. Labels: "Change from last year", "Most notable change". Segregation opened-trail only. Summary = who + when/how. Same PDF URL OK if link text differs. Metro folds approval onto measures; `metro_commitment` for post-vote pledge (e.g. Sky Ranch 2022 Capital Pledge). | `authorityMillsChangeBlocks.ts`, metro pack, `sky-ranch-3-metro-authority-chain`, authoring doc |
| 2026-08-15 | Sky Ranch metro trail: recent YoY first **and** largest material change (2020→2021 0→126.336) via AUTH-derived most-notable block (supersedes hand `historicalComparison`). Formation (2005) not in trail. November elector tax authorization and December county service-plan approval are separate, chronologically ordered facts; both use `$312 million` and explicitly distinguish per-category annual tax authority from aggregate debt cap. August 2022 pledge follows. Omit duplicate unavailable-election fact when approval evidence exists. | `sky-ranch-3-metro-authority-chain` |
| 2026-08-14 | Authority chain: master trail stays for every family (Who gets money, What changed / mills compare, authorization steps, votes/approval when citable, budget, openGaps). Family pack **injects** type pieces; does not remove master pieces. Mills-over-time chart is a **separate** module, not pack scope. Optional school/county `mills.historicalComparison` is trail-only, not the chart. | `levyAuthorityChainTemplates.ts` |
| 2026-08-14 | Metro authority chain: actor = **eligible electors** (+ `term-eligible-electors` popover). No invented Ballot Issue letter; link board packets / budgets / audits / DOLA. Hard facts only. Metro schema: `votes`+`resultsSource` **or** cited `approval`. Spanish/AI = one-off county fallback (Littleton 4C), not a metro component. First entry: Sky Ranch `4571` (`metro_authorization`). | Pack + entry; authority-chain authoring |
| 2026-08-13 | Rent unknown-N / commercial Real friction parked: do **not** hide Rent when N is null or gate on commercial/`UB` for 4.8.2. Soften landlord copy / unknown-N panel later. | `docs/_working/10-rent-unknown-n-commercial-friction.md`. |
| 2026-08-13 | Own\|Rent + Rent dashboard **hidden on BPP**; preference kept when switching back to Real. Unknown-N / pending rent copy uses **units** / **per-unit** (not homes). | `HomeParcelAddressLookup`, `RentTaxPressurePanel`, levy rent footnote. |
| 2026-08-13 | Dashboard **Switch account type** only when situs has business personal **plus** another account kind (typically Real/Improvement). All-Real multi (condos) omit it; Matching properties chooser still lists units. Unit-to-unit compare without restart = parked (`docs/_working/11-same-situs-unit-compare.md`). | `situsShouldOfferAccountTypeSwitch`, `HomeParcelAddressLookup`. |
| 2026-08-12 | Rent pierce **h2** **You're still paying property tax if you rent.** above aside (centered; **no sub-head** — “folded into rent” rejected). Gaps: `HOME_AUDIENCE_STACK_GAP_CLASS` (`space-y-8`) on home section + rent heading stack so toggle↔heading === heading↔tiles. Monthly tile **Your estimated property tax**. | `RentTaxPressurePanel`, `HomePageClient`, `toolFlowStyles`. |
| 2026-08-12 | Rent summary = colored tiles only (prose experiment removed). Mobile **`grid-cols-1` full-width** (not levy `auto-fill`); `sm+` `auto-fit` + 14rem. Levy-matched label/value type; labels may wrap. Gradients ≠ levy. Divider under rent block. | `RentTaxPressurePanel`, `RENT_SUMMARY_*`. |
| 2026-08-13 | Landing Rent: pierce + same-line invite (**You're still paying property tax if you rent. Where's it going?**); no separate sub-head; no "Get a clear picture" / "Even if you rent.". Report rent **h2** stays pierce only. | Supersedes 2026-08-12 landing pierce-only. `HomePageClient`. |
| 2026-08-12 | Landing: Own = line1 + money-going; Rent = pierce only (no “Get a clear picture…”, no “Even if you rent.”). No Own/Rent sentence under switch (aria only). No address autofocus on land. | **Superseded 2026-08-13** for Rent landing second sentence. Own + aria/autofocus still stand. `HomePageClient`, `AudienceModeSwitch`. |
| 2026-08-12 | Home Own \| Rent: default **Own**; always-enabled search; same chrome search + report; Start over → Own; not salmon Switch account type. | `AudienceModeSwitch`, `HomeParcelAddressLookup`. |
| 2026-08-12 | ~~Renter **N**: Land Line `UB` sum → duplex/triplex/fourplex → N=1 single-dwelling → else unknown (whole-account only). Manual fixture PIN `035183106` (not CI).~~ | **Superseded 2026-09-08** (insert building `Units` sum after UB). |
| 2026-08-12 | Rent levy/metro **$**: per-unit when N known, then **monthly /mo** (Own stays annual whole-account). Metro headline = rent framing. No per-tile `*` / `~`; equal-split caveat in rent meta + levy stack footnote. | `levyDisplayDollarsForAudience`, `LevyStackVisualization`, `MetroTaxShareFlow` `rentMode`. |
| 2026-08-12 | Rent: **no Add tile** / levy line edit; omit owner fine print (county notice + Add tile). Hide comps / BPP NOV / demo comps / owner mail / bill-impact banner. Sale/building/land = outlined toggle. | `allowLineEdit: !isRentMode`; curation in lookup / parcel panels. |
| 2026-08-12 | ~~Home audience gate: address disabled until Own or Rent~~ | Superseded: default Own + always-enabled search. |
| 2026-08-01 | Mill history chart: descriptive heading only (**Total mills from county property tax tables**); no caption popover | Supersedes 2026-07-28 popover decision. `AuthorityMillsHistoryChart.tsx`, `levyYoYCopy.ts`. |
| 2026-07-28 (superseded 2026-08-01) | ~~**Mill rate over time** chart: heading + caption in `InfoHintPopover`~~ | Replaced by descriptive heading only. |
| 2026-07-31 | Authority chain panel title stays **Who authorized this?** for now | Declined rename to "Why did this change?" |
| 2026-07-31 | Home `lg+` layout: property details sidebar on the **left**, levy/main on the **right** | Do not swap sides without asking. |
