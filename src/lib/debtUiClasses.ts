/** Debt list card: thin border, square corners (stacks flush inside Result). */
export const DEBT_PANEL_CLASS =
  "overflow-hidden border border-red-500 bg-debt-wash";

/** Metro debt share block in the result summary (full thin red frame). */
export const DEBT_RESULT_STRIP_CLASS = "border border-red-500 bg-debt-wash";
